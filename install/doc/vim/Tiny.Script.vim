setlocal nobuflisted "this will let the buffer no show in the MiniBuffers
setlocal buftype=nofile " this will let the buffer can't be modify
setlocal noswapfile " this will let the buffer new with noswap file
echon 'xxxxxxx' . "\r" " this will let the echo msg apear just replace the last one
redraw | echo 'xxxxxxxxxxx' " this will refresh some thing I think
